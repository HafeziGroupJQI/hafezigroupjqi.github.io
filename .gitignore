.DS_Store
.gitignore
node_modules
public
prof
tsconfig.tsbuildinfo
.obsidian
.quartz-cache
private/
.replit
replit.nix
.quartz/
quartz/cli/tui/dist/
.turbo/
# the vault checkout in CI (content is a committed symlink to ../vault/content; CI repoints it)
vault-src/
.cache/

# Gateway runtime state and credentials
members/.env
members/.venv/
members/data/
**/__pycache__/
**/.pytest_cache/
